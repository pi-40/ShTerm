name: Publish Release ZIP to GitHub Packages

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish-package:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Download Release ZIP
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            ZIP_URL="https://github.com"
          else
            ZIP_URL=$(jq -r '.release.assets[] | select(.name | endswith(".zip")) | .url' $GITHUB_EVENT_PATH)
          fi
          
          curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
               -H "Accept: application/octet-stream" \
               -L -o ShTerm.zip "$ZIP_URL"

      - name: Build Package Archive
        run: |
          mkdir package
          unzip ShTerm.zip -d package/
          
          cat <<EOF > package/package.json
          {
            "name": "@pi-40/shterm",
            "version": "1.0.0",
            "description": "ShTerm package",
            "main": "index.js",
            "publishConfig": {
              "registry": "https://github.com"
            },
            "repository": {
              "type": "git",
              "url": "git+https://github.com"
            }
          }
          EOF
          
          tar -czf shterm-package.tgz package/

      - name: Push to GitHub Packages
        run: |
          echo "//://github.com{{ secrets.GITHUB_TOKEN }}" > ~/.npmrc
          npm publish shterm-package.tgz --registry=https://github.com
